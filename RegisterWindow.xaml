<Window x:Class="InverTrack.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crear cuenta - InverTrack" Height="480" Width="520" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}" ResizeMode="NoResize">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Margin="20" Padding="20">
                <StackPanel>
                <TextBlock Text="Crear nueva cuenta" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,8"/>
                <TextBlock Text="Completa tus datos para comenzar a invertir" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,16"/>

                <TextBlock Text="Usuario" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBox x:Name="TxtUsuario" Padding="8" Margin="0,0,0,8" FontSize="13"
                         Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                <TextBlock Text="Correo electrónico" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBox x:Name="TxtEmail" Padding="8" Margin="0,0,0,8" FontSize="13"
                         Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                <TextBlock Text="Contraseña" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <PasswordBox x:Name="TxtContrasena" Padding="8" Margin="0,0,0,8" FontSize="13"
                             Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                <TextBlock Text="Dinero inicial ($)" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBox x:Name="TxtDinero" Padding="8" Margin="0,0,0,8" FontSize="13" Text="10000"
                         Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                <TextBlock x:Name="LblError" Foreground="{DynamicResource AccentSellBrush}" FontSize="11" Margin="0,4,0,8" TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Cancelar" Width="90" Height="30" Margin="0,0,8,0" Click="BtnCancelar_Click"/>
                    <Button Content="Crear cuenta" Width="110" Height="30" Style="{StaticResource PrimaryButtonStyle}" Click="BtnCrear_Click"/>
                </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Window>
