<Window x:Class="InverTrack.RecoverPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recuperar contraseña" Height="460" Width="520" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}" ResizeMode="NoResize">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Margin="20" Padding="20">
                <StackPanel>
                <TextBlock Text="Recuperar contraseña" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,8"/>
                <TextBlock Text="Ingresa tu correo electrónico para recibir un código de recuperación" FontSize="12" 
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,16" TextWrapping="Wrap"/>

                <TextBlock Text="Correo electrónico" Margin="0,0,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBox x:Name="TxtEmail" Padding="8" Margin="0,0,0,10" FontSize="13"
                         Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                <Button x:Name="BtnEnviarCodigo" Content="Enviar código" Click="BtnEnviarCodigo_Click" 
                        Style="{StaticResource PrimaryButtonStyle}" Height="30" Width="130" HorizontalAlignment="Left" Margin="0,0,0,10"/>

                <StackPanel x:Name="CodigoPanel" Visibility="Collapsed">
                    <TextBlock Text="Código de verificación" Margin="0,10,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBox x:Name="TxtCodigo" Padding="8" Margin="0,0,0,8" FontSize="13"
                             Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                    <TextBlock Text="Nueva contraseña" Margin="0,4,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <PasswordBox x:Name="TxtNuevaContrasena" Padding="8" Margin="0,0,0,8" FontSize="13"
                                 Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                    <TextBlock Text="Confirmar contraseña" Margin="0,4,0,4" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <PasswordBox x:Name="TxtConfirmarContrasena" Padding="8" Margin="0,0,0,8" FontSize="13"
                                 Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource InputBorderBrush}"/>

                    <Button x:Name="BtnConfirmar" Content="Confirmar cambio" Click="BtnConfirmar_Click"
                            Style="{StaticResource PrimaryButtonStyle}" Height="30" Width="150" HorizontalAlignment="Left" Margin="0,6,0,0"/>
                </StackPanel>

                <TextBlock x:Name="LblError" Foreground="{DynamicResource AccentSellBrush}" FontSize="11" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Window>
